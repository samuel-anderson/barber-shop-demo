import styled from "styled-components/native";
import { FlatList } from "react-native";

export const ServiceCardList = styled(FlatList).attrs({
  contentContainerStyle: {
    padding: 5,
    justifyContent: "center",
  },
})``;
