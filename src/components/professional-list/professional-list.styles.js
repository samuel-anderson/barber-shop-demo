import styled from "styled-components/native";
import { FlatList } from "react-native";

export const ProfessionalCardList = styled(FlatList).attrs({
  contentContainerStyle: {
    padding: 16,
  },
})``;
